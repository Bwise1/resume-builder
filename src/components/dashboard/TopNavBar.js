import React from 'react';
import { AppBar, Toolbar } from '@material-ui/core'

const TopNavBar = ()=>{
    return(
        <React.Fragment>
            <AppBar>
                <Toolbar>
                    Hello
                </Toolbar>
            </AppBar>
        </React.Fragment>
    );
}

export default TopNavBar;